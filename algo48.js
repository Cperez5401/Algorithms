function longestPalindromeStr(str) {
    var longsetpalindrome = str[0];
    for (var i = 0; i < str.length; i++) {
        for (var j = 0; j < str.length; j++) {
            var substring = str.slice(i, j);

            if (substring.length > longsetpalindrome.length && is )
        }
    }
}